server.port=8081


# ========================================
# DATASOURCE (COINCIDE CON DOCKER)
# ========================================
spring.datasource.url=jdbc:postgresql://localhost:5432/venues
spring.datasource.username=admin
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver


# ========================================
# JPA / HIBERNATE
# ========================================
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# ========================================
# FLYWAY
# ========================================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true


# === JWT ===
jwt.secret=ESTE_ES_UN_SECRETO_DEMO_QUE_DEBERIA_SER_MAS_LARGO_123456789
jwt.expiration-ms=3600000


# ========================================
# Actuator + Micrometer
# ========================================

management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always

# Etiquetas comunes
management.metrics.tags.application=venues-service

# Habilitar Prometheus
management.metrics.export.prometheus.enabled=true
